import React from "react";
import Grid from "./grid/Grid";

const App = () => {
  return (
    <div>
      <Grid numberOfCards={9}/>
    </div>
  );
};

export default App;

/** components -> icon, card, grid, helper -> rules */
